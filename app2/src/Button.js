import React from 'react';

const Button = () => (
    <button
        onClick={() => {
            console.log('this is App 2 Button');
        }}
    >
        App 2 Button
    </button>
);

export default Button;
